# Python >= 3.8（Windows 10/11 桌面宠物）
# ---- 运行时依赖 ----
PySide6>=6.5,<7
requests>=2.28
psutil>=5.9
# ---- 构建/打包依赖（仅生成 exe 时需要） ----
PyInstaller>=6.0
# ---- 开发期脚本依赖（去背景.py / 生成占位角色.py，运行时不 import） ----
Pillow>=9.0
